fn fibonacci(x) {
  if (x == 0) {
    return 0;
  } else {
    if (x == 1) {
      return 1;
    } else {
      return fibonacci(x - 1) + fibonacci(x - 2);
    }
  }
};

putint(fibonacci(7));
put(10);

putint(fibonacci(1));
put(10);

putint(fibonacci(15));
put(10);
